/**
 * Centralised mock data for POS-Coffee.
 *
 * PC-1 only seeds the file so downstream tasks (PC-2 onward) have a
 * single location to add categories, products, modifiers, and seed
 * users. Components MUST import data from here instead of hard-coding
 * values.
 */

export const APP_NAME = "POS-Coffee"

export const APP_TAGLINE = "A warm point of sale for small coffee shops."
